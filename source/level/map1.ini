[MAPPING]
; Mapper config. 
; Used to make define place of block in final rendering
MAPPER=map1.png
MAPPER_WIDHT=32
MAPPER_HEIGHT=32
; Pattern config.
; Used to make relation between positionning element and texturing element
PATTERN=map1_pattern.png
PATTERN_WIDTH=10
PATTERN_HEIGHT=10
; Render config.
; Used to define rendering texture of elements
RENDER=map1_render.png
RENDER_WIDTH=32
RENDER_HEIGHT=32
; Pattern solid block config.
; Used to define the solid block in maps
PATTERN_SOLID_BLOCK=map1_pattern_solid_block.png
PATTERN_SOLID_BLOCK_WIDTH=10
PATTERN_SOLID_BLOCK_HEIGHT=10
